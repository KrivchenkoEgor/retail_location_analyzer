"""
config.py — центральные настройки приложения Retail Location Analyzer.

Все константы, дефолты демографии, теги OSM и эндпоинты собраны здесь,
чтобы UI и ядро ссылались на единый источник истины.
"""
from __future__ import annotations

import logging

LOG_LEVEL = logging.DEBUG
logging.basicConfig(
    level=LOG_LEVEL,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    datefmt="%H:%M:%S",
)

# ---------------------------------------------------------------------------
# Почта России (geo.pochta.ru) — открытый API без авторизации
# ---------------------------------------------------------------------------
POCHTA_BASE = "https://geo.pochta.ru"
POCHTA_GEOCODE = POCHTA_BASE + "/api/geocode/by-address-string"
POCHTA_IN_CIRCLE = POCHTA_BASE + "/api/address-search/in-circle"
POCHTA_TIMEOUT = 30  # секунд на один запрос к Почте России

# ---------------------------------------------------------------------------
# Nominatim (резервный геокодер) — OpenStreetMap
# ---------------------------------------------------------------------------
NOMINATIM_URL = "https://nominatim.openstreetmap.org/search"
NOMINATIM_TIMEOUT = 15

# ---------------------------------------------------------------------------
# Overpass API — зеркала для отказоустойчивости.
# Перебираются по порядку; при 429/504/таймауте — следующее.
# (источник списка зеркал: competitors.py из корневого проекта)
# ---------------------------------------------------------------------------
OVERPASS_MIRRORS = [
    # Рабочие зеркала — первыми (проверено в сети проекта).
    "https://maps.mail.ru/osm/tools/overpass/api/interpreter",
    "https://overpass-api.de/api/interpreter",
    # Резервные — на случай доступности из другой сети.
    "https://overpass.kumi.systems/api/interpreter",
    "https://overpass.private.coffee/api/interpreter",
]
OVERPASS_TIMEOUT = 20          # сетевой таймаут одного запроса
OVERPASS_QUERY_TIMEOUT = 15    # [timeout:N] внутри Overpass QL
OVERPASS_RETRIES = 1           # число попыток на одно зеркало
OVERPASS_SLEEP_BETWEEN_POINTS = 10  # пауза между точками (сек) — защита от 429

POINT_TIMEOUT = 120            # общий таймаут на анализ одной точки (сек)
USER_AGENT = "retail-location-analyzer/1.0 (geo research)"

# ---------------------------------------------------------------------------
# my-gkh.ru — портал ГИС ЖКХ для обогащения данными о доме
# ---------------------------------------------------------------------------
MYGKH_BASE = "https://my-gkh.ru"
MYGKH_HOUSES_API = MYGKH_BASE + "/housejsonsearchregioncity/novosibirsk"
MYGKH_HOUSE_INFO = MYGKH_BASE + "/gethouse"
MYGKH_TIMEOUT = 15            # секунд на запрос к my-gkh.ru
MYGKH_BOUNDS_RADIUS = 0.005   # градусов вокруг точки для поиска дома (~500м)

# ---------------------------------------------------------------------------
# Радиусы по умолчанию (метры) — настраиваются в UI
# ---------------------------------------------------------------------------
DEFAULT_RADIUS_SHOPS = 500
DEFAULT_RADIUS_OFFICES = 100
DEFAULT_RADIUS_EDUCATION = 150

# ---------------------------------------------------------------------------
# OSM-теги для поиска конкурентов (продуктовые магазины).
# Полный набор — объединение списков из overpass_client.py и competitors.py.
# ---------------------------------------------------------------------------
SHOP_TYPES = (
    "supermarket|convenience|hypermarket|grocery|mini_supermarket|"
    "wholesale|frozen_food|beverages|alcohol|health_food|spices|"
    "butcher|dairy|greengrocer|farm"
)

# Категории продуктовых магазинов для группировки в отчёте.
# Ключ — человекочитаемое название категории, значение — список shop-тегов OSM.
SHOP_CATEGORIES = {
    "Супермаркеты": ["supermarket", "hypermarket", "mini_supermarket"],
    "Магазины у дома": ["convenience", "grocery"],
    "Алкоголь": ["alcohol", "beverages"],
    "Мясные": ["butcher"],
    "Молочные": ["dairy"],
    "Овощи/фрукты": ["greengrocer"],
    "Фермерские": ["farm", "health_food"],
    "Заморозка/опт": ["frozen_food", "wholesale"],
    "Прочее": ["spices"],
}

# ---------------------------------------------------------------------------
# Образование: теги OSM + регулярка по названию для ПТУ/техникумов/училищ.
# amenity=college по соглашению OSM покрывает ССУЗы (включая ПТУ/техникумы).
# Дополнительно ловим по словам в названии (страхуемся от разметки без amenity).
# ---------------------------------------------------------------------------
EDUCATION_AMENITY = "university|college"
EDUCATION_NAME_REGEX = "(колледж|техникум|училище|пту|политехникум)"

# ---------------------------------------------------------------------------
# Демографическая модель — дефолты из bf_house_and_apartments_analysis.xlsx.
# Значения отражают структуру домохозяйств Омска; в UI редактируемые.
# ---------------------------------------------------------------------------
# Среднее число жителей на квартиру (базовая оценка).
DEFAULT_AVG_PERSONS_PER_FLAT = 2.3
# Доля одиноких в населении (поправка к базовой оценке).
DEFAULT_SINGLE_SHARE = 0.285  # 28,5%
# Распределение домохозяйств по числу человек (доли, в сумме = 1.0).
# Ключ — число человек, значение — доля домохозяйств.
DEFAULT_HOUSEHOLD_DISTRIBUTION = {
    1: 0.235,  # одиночки — 23,5%
    2: 0.300,  # пары — 30%
    3: 0.230,  # семьи с 1 ребёнком — 23%
    4: 0.150,  # семьи с 2 детьми — 15%
    5: 0.085,  # крупные семьи (5+) — 8,5%
}

# ---------------------------------------------------------------------------
# Сетевые конкуренты для матрицы присутствия (как в образце Excel).
# При группировкеPOI эти имена сворачиваются в верхний регистр/нормализуются.
# ---------------------------------------------------------------------------
KEY_CHAINS = [
    "Магнит", "Красное & Белое", "Пятёрочка", "Бирхаус", "Ярче!",
    "Победа", "Eurospar", "Fix Price", "Тюкалиночка",
]
